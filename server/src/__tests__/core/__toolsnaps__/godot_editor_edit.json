{
  "name": "godot_editor_edit",
  "description": "Drive the editor: select a node, run or stop the project, restart the editor, and center/zoom the 2D viewport. Use run with frozen=true as the deterministic-playtest entry point (game time holds at frame 0 until godot_game_time steps or thaws it), and use restart to recover when externally edited .gd files or a stale project.godot have left the running editor out of sync with disk. For observation only (state, selection, logs, screenshots) use godot_editor_read instead; restart does not start a cold editor, so one must already be running.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "select",
          "run",
          "stop",
          "restart",
          "set_viewport_2d"
        ],
        "description": "select: Select a node in the editor\nrun: Run the project\nstop: Stop the running project\nrestart: Restart the editor, reloading project.godot (autoloads, input map), addon code, and plugins from disk. Use after external edits the running editor would otherwise keep stale. Fire-and-forget: the bridge drops and auto-reconnects within a few seconds. Does not start a cold editor - the editor must already be running.\nset_viewport_2d: Center and zoom the 2D editor viewport. Pass at least one parameter — and note omitted parameters RESET to defaults (center 0,0 / zoom 1.0) rather than keeping the current view, so pass all three to change just one dimension predictably."
      },
      "node_path": {
        "type": "string",
        "description": "Path to node to select (required for: select)"
      },
      "scene_path": {
        "description": "Scene to run (optional, defaults to main scene) (for: run)",
        "type": "string"
      },
      "frozen": {
        "description": "Launch with game time frozen from frame 0 (gameplay never starts racing your latency). Use godot_game_time step/thaw to advance. (for: run)",
        "type": "boolean"
      },
      "save": {
        "description": "Save the project before restarting (default: true). Set false to discard unsaved editor changes. (for: restart)",
        "type": "boolean"
      },
      "center_x": {
        "description": "X coordinate to center the 2D viewport on (omitted = resets to 0) (for: set_viewport_2d)",
        "type": "number"
      },
      "center_y": {
        "description": "Y coordinate to center the 2D viewport on (omitted = resets to 0) (for: set_viewport_2d)",
        "type": "number"
      },
      "zoom": {
        "description": "Zoom level, e.g. 1.0 = 100%, 2.0 = 200% (omitted = resets to 1.0) (for: set_viewport_2d)",
        "type": "number",
        "exclusiveMinimum": 0
      }
    },
    "required": [
      "action"
    ]
  },
  "annotations": {
    "title": "Editor Control (edit)",
    "readOnlyHint": false,
    "destructiveHint": false,
    "openWorldHint": false
  }
}
