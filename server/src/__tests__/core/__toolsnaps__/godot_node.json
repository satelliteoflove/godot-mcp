{
  "name": "godot_node",
  "description": "Inspect and modify scene nodes in the editor: read effective properties (including class defaults a .tscn read cannot show), view the full scene tree, find nodes, update properties, and reparent (the editor rewrites child paths and signal connections correctly; hand-editing .tscn for a reparent does not). To add or remove nodes, or attach scripts and connect signals, edit the .tscn file directly, then verify with get_scene_tree.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "get_properties",
          "get_scene_tree",
          "find",
          "update",
          "reparent"
        ],
        "description": "get_properties: Get a node's properties\nget_scene_tree: Full hierarchy of the open scene as the editor sees it, including children inside instanced sub-scenes (a .tscn file read cannot show those)\nfind: Find nodes by name and/or type\nupdate: Update a node's properties\nreparent: Move a node to a new parent"
      },
      "node_path": {
        "type": "string",
        "description": "Path to the node (required for: get_properties, update, reparent)"
      },
      "name_pattern": {
        "description": "Glob pattern to match node names, e.g. \"*Spawner*\", \"Turret?\" (for: find)",
        "type": "string"
      },
      "type": {
        "description": "Filter by node type, e.g. \"CharacterBody2D\", \"Area2D\" (for: find)",
        "type": "string"
      },
      "root_path": {
        "description": "Path to start search from (defaults to scene root) (for: find)",
        "type": "string"
      },
      "properties": {
        "description": "Properties to set on the node (for: update)",
        "type": "object",
        "propertyNames": {
          "type": "string"
        },
        "additionalProperties": {}
      },
      "new_parent_path": {
        "type": "string",
        "description": "Path to the new parent node (required for: reparent)"
      }
    },
    "required": [
      "action"
    ]
  },
  "annotations": {
    "title": "Node",
    "readOnlyHint": false,
    "destructiveHint": false,
    "openWorldHint": false
  }
}
