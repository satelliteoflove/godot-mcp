{
  "name": "godot_resource",
  "description": "Inspect a Resource file by path with type-aware structured output (SpriteFrames animations, TileSet, Material, Texture2D, etc.). Use it for imported or binary resources (.res, .scn, compressed textures) that a plain file read cannot parse, or when you want sub-resources resolved into the engine's view rather than raw .tres text. For nodes inside a scene, use godot_node_read instead.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "get_info"
        ],
        "description": "get_info: Inspect a Resource file by path"
      },
      "resource_path": {
        "type": "string",
        "description": "Resource path (e.g., \"res://player/sprites.tres\")"
      },
      "max_depth": {
        "description": "Detail level: 0 = summary only, 1 = full detail (default), 2+ = expand sub-resources",
        "type": "number"
      },
      "include_internal": {
        "description": "Include internal properties starting with underscore (default: false)",
        "type": "boolean"
      }
    },
    "required": [
      "action",
      "resource_path"
    ]
  },
  "annotations": {
    "title": "Resource Inspector",
    "readOnlyHint": true,
    "openWorldHint": false
  }
}
